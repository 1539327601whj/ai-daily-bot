# 2026-04-28 日志

## 完成的工作

### 简报时间显示修复
- 后端 `ReportServiceImpl.java` - 使用 `ZoneId.of("Asia/Shanghai")` 设置时区
- 前端 `Dashboard.tsx` - 使用 dayjs 格式化时间 `MM-DD HH:mm`
- 前端 `History.tsx` - 使用 dayjs 格式化日期和时间

### Vercel 部署修复
- 登录 Vercel 账号（token 已过期重新登录）
- 修复 TypeScript 构建错误（移除未使用的 import 和变量）
- 成功部署前端到生产环境

## 部署状态

| 平台 | 状态 | URL |
|------|------|-----|
| 后端 (Render) | ✅ 运行中 | https://ai-daily-bot.onrender.com |
| 前端 (Vercel) | ✅ 已部署 | https://frontend-pearl-phi-70.vercel.app |

## 遇到的问题

1. **Vercel token 过期** - 需要重新执行 `npx vercel login`
2. **TypeScript 构建错误 TS6133** - 未使用的 import 和变量导致构建失败
   - 修复：移除 Dashboard.tsx 中的 `latest` 变量
   - 修复：移除 Subscription.tsx 中的 `navigate` 变量  
   - 修复：移除 ReportDetail.tsx 中未使用的 `dayjs` import

### 简报时间时区修复（第二次）
- **问题**：GitHub Actions 默认使用 UTC 时区，导致简报生成时间和版本判断错误
- **修复内容**：
  - `.github/workflows/daily.yml` - 添加 `TZ: 'Asia/Shanghai'` 环境变量
  - `scripts/daily_report.py` - 使用 `datetime.now(BEIJING_TZ)` 获取北京时间
  - `frontend/src/utils/dayjs.ts` - 新增 dayjs 时区配置，默认使用 Asia/Shanghai
  - `Dashboard.tsx`, `History.tsx`, `ReportDetail.tsx` - 使用配置的 dayjs

### 项目漏洞检查与修复
- **发现的问题**：
  1. GitHub Actions 定时任务被注释，简报不会自动发送
  2. Dashboard 订阅时间显示错误（显示 08:00/20:00，实际是 07:00/19:00）
  3. Chat 组件缺少空值处理，可能导致页面崩溃
- **修复内容**：
  - `.github/workflows/daily.yml` - 取消注释定时任务，启用每日 07:00 和 19:00 自动发送
  - `Dashboard.tsx` - 修正订阅时间显示为 07:00 和 19:00
  - `Chat.tsx` - 添加空值处理 `result.data?.answer || '抱歉，未能获取有效回答'`
- **提交信息**：`fix: 启用定时任务 + 修复订阅时间显示 + Chat空值处理`

### 简报时间调整（第二次）
- **用户需求**：改回 08:00 和 20:00，由腾讯云 SCF 触发 GitHub workflow
- **修复内容**：
  - `Dashboard.tsx` - 订阅时间改为每日 08:00 和 20:00
  - `.github/workflows/daily.yml` - 恢复 schedule 注释（使用腾讯云 SCF 触发）
# 2026-04-28 工作日志

## Render Docker 构建修复
- **问题**: Docker 构建时找不到 pom.xml 和 src
- **原因**: 构建上下文是 GitHub 根目录，但 Dockerfile 在 backend/ 子目录
- **修复**: 在 render.yaml 中添加 

## Vercel 部署被阻止修复
- **问题**: 部署被阻止，提示提交作者邮箱无效
- **原因**: Git 配置的邮箱 (1539327601qq.com) 与 GitHub 账户邮箱不一致
- **修复**: 修改 Git 配置为 ，并 amend 提交强制推送

